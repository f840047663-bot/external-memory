# 00-网站内容抓取总纲（三平台数据获取规则）

> 最后更新：2026-05-25（第4次重写）
> 状态：✅ 已验证三平台当前均跑通
> ⚠️ 铁律：本文档写死当前实际工作方法。技术换了就改本文档，不改等于白写。

---

## 一、三平台总览（精确到脚本路径）

| 平台 | 脚本 | cron | 博主来源 | Cookie来源 | 状态 |
|:----|:----|:----|:---------|:----------|:----:|
| **B站** | `~/.hermes/scripts/bilibili_monitor.py` | 13:00 no_agent | `thinkers/INDEX.md`第四列`mid:数字` | `~/桌面/凭证/bilibili_com_cookie_最新.txt` | ✅ |
| **抖音** | `~/.hermes/scripts/douyin-monitor.py` | 13:00 no_agent | `thinkers/INDEX.md`第三列`MS4w...sec_uid` | CDP提cookie → 兜底~/桌面/凭证/ | ✅ |
| **知乎** | `~/.hermes/scripts/zhihu_monitor.py` | 13:10 no_agent | `thinkers/INDEX.md`第五列`people/xxx` | `~/桌面/凭证/zhihu_com_cookie_最新.txt` | ✅ |

**抖音cron注意：** 2026-05-25从旧版`douyin_monitor.py`换到新版`douyin-monitor.py`。旧脚本只从`~/桌面/配置清单/抖音主页.txt`读3个人，新脚本从INDEX.md读全部12人。

---

## 二、各平台详细流程

### B站（bilibili_monitor.py）

**启动方式：** cron每日13:00自动跑（job: c964de258fbe, no_agent）

**博主列表读取：**
1. 优先读 `INDEX.md` 第四列（B站列），格式 `mid:数字`
2. 兜底读 `~/桌面/配置清单/B站监控清单.txt`

**Cookie加载：**
1. 读 `~/桌面/凭证/bilibili_com_cookie_最新.txt`（primary）
2. 兜底：`yt-dlp --cookies-from-browser chrome --cookies /tmp/bili_latest_cookies.txt`

**API调用：**
```bash
curl "https://api.bilibili.com/x/space/arc/search?mid={mid}&ps=30" \
  -H "Cookie: $(cat ~/桌面/凭证/bilibili_com_cookie_最新.txt)"
```

**速度：** ~5秒完成。偶发-799（请求过频），等10秒重试1次即可。

**⚠️ 请求频率铁律：** 不同博主API请求之间必须加**sleep(5-8秒)**。连续快速请求会触发-799限流。手工跑时尤其注意——cron no_agent模式单线程自带间隔，但手工逐个查时容易太快。

**输出：** `~/桌面/每日视频总结/2026-05-25_B站报告.md`

**如果API全挂（全部-799）：**
```bash
browser_navigate(url="https://space.bilibili.com/{mid}")
```

---

### 抖音（douyin-monitor.py）

**启动方式：** cron每日13:00自动跑（job: c7d80867ebbe, no_agent, 脚本365行）

**博主列表读取：**
1. 读 `INDEX.md` 第三列（抖音列），匹配反引号包裹的 `MS4w...` sec_uid
2. 不从 `~/桌面/配置清单/抖音主页.txt` 读（已废弃）

**Cookie提取（Chrome v147+专属流程）：**
1. Chrome调试模式必须提前启动在9222端口（`~/桌面/系统工具/启动Chrome调试模式.sh`，双击或在终端用`.desktop`启动）
2. 脚本自动走CDP连接Chrome → 创建标签页 → 导航抖音首页 → 等6秒 → Network.getAllCookies
3. 提取成功的cookie含`sessionid` → 存入 `/tmp/douyin_cookies.txt`
4. 兜底读 `~/桌面/凭证/douyin_com_cookie_最新.txt`

**API调用：**
```bash
curl "https://www.douyin.com/aweme/v1/web/aweme/post/?sec_user_id={sec_uid}&count=30&aid=1128" \
  -H "Cookie: $(cat /tmp/douyin_cookies.txt)" \
  -H "User-Agent: Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/147.0.0.0 Safari/537.36" \
  -H "Referer: https://www.douyin.com/user/{sec_uid}"
```
**头部铁律（不遵守会误判空列表）：**
- ⚠️ **User-Agent必须用完整Chrome版本号**，不能用 `Mozilla/5.0`（太笼统→API可能不返回数据）
- ⚠️ **Referer必须带完整 `/user/{sec_uid}`**，光 `https://www.douyin.com/` 不行
- ⚠️ **count必须≥30**，小count值（如count=5）对某些账号（但斌）返回0条

**输出：** 直接stdout打印（no_agent模式→cron捕获）

**已知问题：**
- 部分博主（付鹏）偶尔API返回空 `status_code:5` → 跳过
- CDP导航页面需等6秒，扫描12人约需60秒

---

### 知乎（zhihu_monitor.py）

**启动方式：** cron每日13:10自动跑（job: 595e1a1dd15e, no_agent）

**用户列表读取：**
1. 读 `INDEX.md` 第五列（知乎列），格式 `people/xxx`
2. 兜底读 `~/桌面/配置清单/知乎监控清单.txt`

**Cookie加载：**
1. 读 `~/桌面/凭证/zhihu_com_cookie_最新.txt`（primary）
2. 兜底：`yt-dlp --cookies-from-browser chrome --cookies /tmp/zhihu_latest_cookies.txt`

**API调用：**
```bash
# 文章列表
curl "https://www.zhihu.com/api/v4/members/{url_token}/articles?limit=10" \
  -H "Cookie: $(cat ~/桌面/凭证/zhihu_com_cookie_最新.txt)" \
  -H "User-Agent: Mozilla/5.0 (iPhone; CPU iPhone OS 16_0 like Mac OS X)"
```
> ⚠️ `{url_token}` 就是纯用户名（如 `gududatong`、`lrc-8`），**不加 `people/` 前缀**。`people/xxx` 是网页版URL格式，API不能用。

```bash
# 回答列表
curl "https://www.zhihu.com/api/v4/members/{url_token}/answers?limit=10\&order_by=created" \
  -H "Cookie: $(cat ~/桌面/凭证/zhihu_com_cookie_最新.txt)" \
  -H "User-Agent: Mozilla/5.0 (iPhone; CPU iPhone OS 16_0 like Mac OS X)"
```

**偶尔空响应/502处理：** 知乎API间歇性返回空（尤其连续请求时），**不是cookie过期**。sleep(5-8秒)后重试一次即可恢复。不要一看到空响应就觉得是cookie问题。
```

**速度：** ~10秒完成（需iPhone UA绕过WAF）

**⚠️ 请求频率铁律：** 不同用户API请求之间必须加**sleep(5-8秒)**。知乎WAF敏感，连续请求容易触发403拦截。

**输出：** `~/桌面/每日视频总结/2026-05-25_知乎报告.md`

**如果API挂了：**
```bash
# 不用搜索，直接拼URL
browser_navigate(url="https://www.zhihu.com/people/{url_token}/articles")
```

---

## 三、Cookie提取（统一流程）

**Chrome v147+只能用CDP，任何python/yt-dlp/playwright方案都废了：**

### 启动Chrome调试模式
**只有一个脚本：** `~/桌面/系统工具/启动Chrome调试模式.sh`（其他Chrome脚本已删除）
**启动方式：** 在系统工具文件夹里**双击「启动Chrome调试模式.sh」**
**验证：** 终端运行 `curl http://127.0.0.1:9222/json/version` 看返回
**注意：** terminal里直接跑会因X权限问题失败，必须从桌面图形环境双击启动
**🤷 如果Hermes自己搞不定：** 直接喊用户「请双击桌面→系统工具→启动Chrome调试模式.sh」，等8秒再连

### 如果没有图形界面可用：
```bash
# 用xauth获取认证
xauth merge /home/fw/.Xauthority
export DISPLAY=:0
# 或直接用headless模式（不需要显示器）
google-chrome --headless --remote-debugging-port=9222 --no-sandbox --user-data-dir=$HOME/.config/google-chrome-debug --disable-gpu &
```

### 提取cookie
```bash
# 1. 先确保Chrome调试模式运行中
curl http://127.0.0.1:9222/json/version

# 2. 提取cookie
python3 ~/.hermes/scripts/cdp-get-cookies.py \
  --domain douyin.com \
  --output ~/桌面/凭证/

# 3. 验证文件存在
ls -la ~/桌面/凭证/{bilibili,zhihu,douyin}_com_cookie_最新.txt
```

**三个cookie文件位置（写死，不动路径）：**
```
~/桌面/凭证/bilibili_com_cookie_最新.txt  ← 972B（2026-05-25）
~/桌面/凭证/douyin_com_cookie_最新.txt   ← 6.2KB（2026-05-25）
~/桌面/凭证/zhihu_com_cookie_最新.txt    ← 1.6KB（2026-05-25）
```

**有效期：** 7-30天，过期后需用户重新登录Chrome一次，再跑cdp-get-cookies.py

---

## 四、三铁律（不遵守会出bug且不告诉你哪错了）

### 铁律1：博主来源统一从INDEX.md读

**所有脚本只能从一个地方读博主列表：`~/.hermes/external_memory/thinkers/INDEX.md`**（不再读桌面配置清单）

| 脚本 | 读哪列 | 格式 |
|:----|:------|:----|
| bilibili_monitor.py | 第四列（B站）—— `thinkers/INDEX.md` | `mid:数字` |
| douyin-monitor.py | 第三列（抖音）—— `thinkers/INDEX.md` | `MS4w...` |
| zhihu_monitor.py | 第五列（知乎）—— `thinkers/INDEX.md` | `people/xxx` |

**禁止行为：** ❌ 从桌面txt读 ❌ 从单独的配置清单读 ❌ 在脚本里写死

### 铁律2：API不行就切浏览器，不绕圈不重试

```
API挂了 → sleep(10) → 重试1次
  ├─ 过了 → 继续
  └─ 不行 → 浏览器拼URL直进主页
             URL从INDEX.md取（mid/sec_uid/people），不搜
```

### 铁律3：说"无更新"必须带证据

```
✅ 付鹏 → 最新: 聊A股近期走势 | 05-22 → 已处理
✅ 宋鸿兵 → API返回空（code=-799过频），浏览器确认最新是05-22
❌ 无新增（没有来源说明）
```

---

## 五、已废弃不要做的事

| 废弃方案 | 原因 |
|:--------|:-----|
| `dy_monitor.py` / `old_douyin_monitor.py` | 已换到`douyin-monitor.py`（CDP版） |
| yt-dlp提取cookie | Chrome v147+ v11加密不可破 |
| source_credibility.py | 用户骂"当数据库整"已删 |
| 独立txt监控清单 | INDEX.md是唯一主数据源 |
| 爬虫/scrapy/selenium | 太重，API+浏览器够用 |
| 在cron里放多线程 | 内存7G撑不住，no_agent单线程 |

---

## 六、崩溃恢复流程（出事看这里）

| 故障现象 | 原因 | 修复 |
|:--------|:----|:----|
| B站全是-799 | API限流，等10-30分钟恢复 | 手动跑时放sleep(15)重试 |
| 抖音全是status_code:5 | cookie过期 / CDP连不上Chrome | 重跑cdp-get-cookies.py |
| 知乎API返回403/空 | cookie过期 | 用户重新登录Chrome→cdp-get-cookies.py |
| 所有平台全挂 | Chrome调试模式没开 | `~/桌面/系统工具/启动Chrome调试模式.sh`（双击全名这个，别点错） |
| cron no_agent报错 | 脚本路径不对 | 检查job的script字段，相对路径=~/.hermes/scripts/下 |
|
|---|
|
|## 七、排错档案（2026-05-27 踩坑实录）|
|
|> 每次新踩的坑都写在这里，下次遇到先查这里。**
|
|### 1️⃣ 抖音：count值太小 → 误判"空列表/私密账号"|
|**症状：** API返回 `status_code:0` 但 `aweme_list:[]`（空列表），误以为账号私密/被删/API挂了|
|**踩坑：** 2026-05-27 但斌。手动 `count=5` 时返回0条，误判为"但斌没有公开作品"。用 `count=30` 后正常返回17条。|
|**真相：** 部分账号（但斌）对 `count` 参数敏感—— 请求太少条就不返回。 |
|**修复：** 永远用 `count=30`。禁用 `count=5`/`count=10`。|
|
|### 2️⃣ 抖音：User-Agent太笼统 → "空响应"|
|**症状：** 同样参数只改了UA就从正常变空 |
|**踩坑：** 用了 `Mozilla/5.0`（不指定浏览器版本）→ 但斌返回0条。换完整UA → 17条|
|**修复：** 永远用 `Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/147.0.0.0 Safari/537.36`|
|
|### 3️⃣ 抖音：Referer不带user路径 → "空响应"|
|**症状：** API正常但返回条数少或空|
|**踩坑：** Referer用 `https://www.douyin.com/` → 不够精确。必须用 `https://www.douyin.com/user/{sec_uid}`|
|**修复：** Referer精确到用户主页 `/user/{sec_uid}`|
|
|### 4️⃣ 抖音：API排序不是时间倒序 → 误报错位|
|**症状：** API列表第1条是1月的旧视频，最新视频在第3位。直接拿第1条vs断点→误报"新内容"|
|**踩坑：** 付鹏的API列表[1]是1月旧视频，最新(断点)在[3]|
|**修复：** 先搜断点ID在列表中的位置，再逐个比ID大小（抖音ID单调递增→ID大=更新）。不能拿"第1条非置顶"直接比对。|
|
|### 5️⃣ 抖音：ID不严格单调递增 → 双验证|
|**症状：** 个别视频ID更大但时间更早|
|**踩坑：** 蒋宇飞第5条视频ID(764433818...) > 断点ID(764433732...)，但时间(09:26)早于断点(12:16)|
|**修复：** 锚点比对必须ID+时间双验证——先找到断点ID，再看列表中ID更大+时间更新的才算新。|
|
|### 6️⃣ JSON解析失败 ≠ API挂了（子进程超时）|
|**症状：** 批量脚本中部分账号报"JSON解析失败"，但单独curl正常|
|**踩坑：** 猪猪女王/但斌/长坡厚雪在批处理脚本中超时报错，但手动curl正常（数据体量大→子进程15秒不够）|
|**修复：** 子进程timeout设120秒以上。先用 `python3 -c "json.load(sys.stdin)"` 验证JSON有效。|
|
|### 7️⃣ 知乎：文章vs回答必须分开追踪|
|**症状：** 只追踪了一个类型（比如只记回答ID），另一类型的断点没了，下次查发现"新内容"|
|**踩坑：** 环中星鉴断点只记了文章ID→漏了1条回答新内容|
|**修复：** 每个知乎用户拆两行（文章+回答）各自独立追踪。见 `监控处理断点.md`。|
|
|### 8️⃣ sleep不足→限流/空响应（全平台通用）|
|**症状：** 跑几个人之后API开始返回空/异常|
|**踩坑：** 手工跑时sleep(5)不够→B站-799、知乎502、抖音status_code:0但空列表|
|**修复：** sleep(12)。用户说不着急，一次跑好比反复重试省时间。|

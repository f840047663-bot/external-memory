# 监控统筹框架

> **最后更新：** 2026-05-31
> **设计原则：** 状态驱动。AI不需要"想怎么走"，只需要"读状态→执行→写状态"。
> **⚠️ 本文档是调度器，<100行。每个模块只写"调哪个子模块"，不重复内容。**

---

## 执行顺序

```
[环境准备] → [抓取/阶段一] → [转录/阶段二] → [分析/阶段三] → [收尾/阶段四]
                                                     ↓
                                           有API失败项且用户同意？
                                                     ↓ 是
                                            [浏览器/兜底]
                                                     ↓
                                            回到对应阶段重试
```

**遇到障碍？** → 调 [决策树/排错]，不要自己猜。

---

## 状态文件：`/tmp/monitor_state.json`

每个模块执行完**必须写**，下一个模块**必须读**。

```json
{
  "当前Step": "环境准备完成",
  "Cookie": {"抖音": "✅", "B站": "✅", "知乎": "❌"},
  "开关": {"调试": "要", "宏观": "不要", "待重仓": "不要", "三关": "不要"},
  "抓取结果": {}, "失败项": [], "待转录": [], "待分析": [],
  "转录结果": [], "贝叶斯结果": [], "断点已更新": false
}
```

---

## 模块路由表（按顺序执行，不要跳）

| 模块 | 子模块文件 | 输入 | 输出 |
|:----|:----|:----|:----|
| **1.环境准备** | `工作流模块/通用/环境准备.md` | 无 | Cookie状态+开关→写状态文件 |
| **2.抓取/阶段一** | `工作流模块/抓取/阶段一.md` | 读状态文件 | 抓取结果+失败项+待转录→写状态文件 |
| ↳ 抖音子模块 | `工作流模块/抓取/抖音.md` | sec_uid+断点 | 新视频→videos/→写状态清单 |
| ↳ B站子模块 | `工作流模块/抓取/B站.md` | mid+断点 | 新视频→videos/→写状态清单 |
| ↳ 知乎子模块 | `工作流模块/抓取/知乎.md` | url_token+断点 | 新文章→texts/→写状态清单 |
| **3.转录/阶段二** | `工作流模块/转录/阶段二.md` | 待转录+失败项 | 转录结果+待分析→写状态文件 |
| **4.分析/阶段三** | `工作流模块/分析/阶段三.md` | 待分析列表 | 贝叶斯结果+归档→写状态文件 |
| **5.收尾/阶段四** | `工作流模块/收尾/阶段四.md` | 全部结果 | 断点+日报+汇报→写状态文件 |
| **6.浏览器/兜底** | `工作流模块/浏览器/兜底.md` | 失败项 | 成功项+仍失败项 |
| **7.决策树/排错** | `工作流模块/通用/决策树排错.md` | 错误信息 | 解决方案或"问用户" |

---

## 执行规则

1. **按顺序**，不跳步。每步输出结果，等用户说"继续"
2. **阶段一失败项** → 问"是否浏览器兜底？" → 是→调模块6→回阶段一重试
3. **全部完成** → 模块5自检10项 → 汇报用户
4. **中断恢复** → 读`/tmp/monitor_state.json` + `/tmp/monitor_status.md`，从断点继续
5. **遇到问题** → 调模块7，不自己猜

---

## 强制自检清单（模块5执行，缺任何一条不准发消息）

```
□ ①主页ID从INDEX.md读的  ②长期主题≥7天搜了  ③P值链可追溯
□ ④P'虚值标了  ⑤events写了  ⑥hindsight_retain存了
□ ⑦状态清单写了  ⑧失败项重试了  ⑨内容存暂存目录  ⑩三平台交替了
```

---

## 关联文件索引

| 文件 | 作用 | 何时读 |
|:----|:----|:----|
| `监控全流程-刻下来.md` | 完整执行手册（968行，参考用） | 子模块没覆盖的细节查这里 |
| `00-网站内容抓取总纲.md` | 三平台API规则+排错（299行，参考用） | 子模块没覆盖的细节查这里 |
| `监控处理断点.md` | 当前断点数据 | 阶段一开始时 |
| `thinkers/INDEX.md` | 博主ID主数据源 | 阶段一开始时 |
| `/tmp/monitor_state.json` | 运行时状态 | 每个模块读写 |
| `/tmp/monitor_status.md` | 抓取状态清单 | 阶段一每处理一个博主写一行 |
